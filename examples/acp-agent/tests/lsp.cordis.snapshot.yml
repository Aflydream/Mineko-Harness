# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - id: llm-deepseek
        name: '@aflydream/mnh-llm-deepseek'
        disabled: true
      - insert:
          - id: lsp
            name: '@aflydream/mnh-lsp'
          - id: lsp-stdio
            name: '@aflydream/mnh-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@aflydream/mnh-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@aflydream/mnh-tool-lsp'
            config:
              maxLocations: 1
          - id: llm-replay
            name: '@aflydream/mnh-llm-replay'
