# Unattended coding-agent deployment for the bundled mnh-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@aflydream/mnh-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.MNH_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.MNH_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: '@aflydream/mnh-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@aflydream/mnh-subprocess-local'

- id: bash
  name: '@aflydream/mnh-bash-local'
  config:
    cwd: !!js process.env.MNH_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@aflydream/mnh-agent-spine-demo'
  config:
    persona: !!js process.env.MNH_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@aflydream/mnh-session-persistence-jsonl'
  config:
    root: !!js process.env.MNH_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.MNH_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@aflydream/mnh-session-checkpoint-policy'

- id: subagent
  name: '@aflydream/mnh-subagent'

- id: subagent-spawn-in-process
  name: '@aflydream/mnh-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@aflydream/mnh-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@aflydream/mnh-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@aflydream/mnh-fs-local'
  config:
    cwd: !!js process.env.MNH_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@aflydream/mnh-fs-observation-policy'

- id: tool-fs
  name: '@aflydream/mnh-tool-fs'

- id: token-meter
  name: '@aflydream/mnh-token-meter'

- id: compaction-basic
  name: '@aflydream/mnh-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
