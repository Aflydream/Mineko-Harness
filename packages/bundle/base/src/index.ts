/**
 * @aflydream/mnh-base — the shared mnh core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `mnh.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @aflydream/mnh-base
 */

export {}
