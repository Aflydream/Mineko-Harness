/**
 * @aflydream/mnh-desktop-app - the Electron desktop patch layer over the
 * shared Web client roster. The package's substance is `cordis.patch.yml`,
 * declared by the `mnh.bundle.patch` manifest field; this module carries no
 * runtime API.
 * @module @aflydream/mnh-desktop-app
 */

export {}
