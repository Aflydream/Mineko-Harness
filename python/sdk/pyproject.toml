[build-system]
requires = ["hatchling==1.30.1"]
build-backend = "hatchling.build"

[project]
name = "mineko-harness-sdk"
version = "0.0.0.dev0"
description = "Python SDK for MiNeko Harness"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Aflydream" }]
dependencies = [
  "pydantic>=2.12,<3",
  "mineko-harness-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/aflydream/mineko-harness"
Documentation = "https://github.com/aflydream/mineko-harness/blob/main/docs/user/guide/python-sdk.md"
Issues = "https://github.com/aflydream/mineko-harness/issues"
Source = "https://github.com/aflydream/mineko-harness"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/mineko_harness"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
mineko-harness-runtime-bin = { path = "../sdk-runtime", editable = true }
